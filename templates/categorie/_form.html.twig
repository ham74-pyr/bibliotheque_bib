{{ form_start(form,{'attr': {'style': 'color: white;', 'class': 'mx-3 mt-3'}}) }}

    {{ form_widget(form) }}
    <button class="btn btn-dark col-12 mt-3 ">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
