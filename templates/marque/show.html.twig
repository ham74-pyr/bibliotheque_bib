{% extends 'base.html.twig' %}

{% block title %}Etat{% endblock %}

{% block body %}
    <h1>Marque</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ marque.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ marque.nom }}</td>
            </tr>
        </tbody>
    </table>

<a href="{{ path('app_marque_index') }}" style="btn btn-secondary">Retour</a>


<a href="{{ path('app_marque_edit', {'id': marque.id}) }}" style="btn btn-secondary">Editer</a>


    {{ include('marque/_delete_form.html.twig') }}
{% endblock %}
