{% extends 'base.html.twig' %}

{% block title %}Matiere{% endblock %}

{% block body %}
<h1>Format</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ matiere.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ matiere.nom }}</td>
            </tr>
        </tbody>
    </table>

<a href="{{ path('app_matiere_index') }}" class="btn btn-primary p-1 mb-1">afficher</a>


<a href="{{ path('app_matiere_edit', {'id': matiere.id}) }}" class="btn btn-secondary p-1 mb-1">editer</a>



    {{ include('matiere/_delete_form.html.twig') }}
{% endblock %}
